apiVersion: v1
kind: Service
metadata:
  name: initialother-frontend-service
  namespace: initialother
spec:
  selector:
    app: initialother-frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP