apiVersion: v1
kind: Service
metadata:
  name: initialother-elasticsearch-service
  namespace: initialother
spec:
  selector:
    app: initialother-elasticsearch
  ports:
    - port: 9200
      targetPort: 9200
  type: ClusterIP