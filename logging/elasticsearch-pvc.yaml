apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: initialother-elasticsearch-pvc
  namespace: initialother
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi